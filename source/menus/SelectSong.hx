package menus;

import flixel.FlxSubState;

class SelectSong extends FlxSubState {

    var curSelected:Int = 0;

    public function new() {
        super();

        
    }

    // gonna do this later but basically it's gonna look like this
    /**
        ballistic-(hq)/hard.json
        fill up/hard.json
        test/normal.json
    **/
    // the curSelected is green, selecting loads the song
}